# Copy to .env.local and fill in values.
# For local dev with Docker: start with `docker compose up -d`, then use the values below.

# Database — use these vars or set DATABASE_URL directly.
# To use an existing DB (e.g. digilocker), set DB_DATABASE=digilocker
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=hospital_tokens

# NextAuth (required for login)
NEXTAUTH_SECRET=your-secret-at-least-32-chars-long-for-dev
NEXTAUTH_URL=http://localhost:3000
